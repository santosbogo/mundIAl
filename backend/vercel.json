{
  "installCommand": "pip install -r requirements.txt",
  "buildCommand": "python -m alembic upgrade head && python -m app.seed.seed",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index" }
  ]
}
