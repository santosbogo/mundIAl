{
  "installCommand": "pip install uv && uv export --no-dev --no-hashes -o requirements.txt && pip install -r requirements.txt",
  "buildCommand": "python -m alembic upgrade head && python -m app.seed.seed",
  "functions": {
    "api/index.py": {
      "maxDuration": 30
    }
  },
  "routes": [
    { "src": "/(.*)", "dest": "api/index.py" }
  ]
}
